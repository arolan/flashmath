<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".SettingsActivity" >

    <EditText
        android:id="@+id/etProfileName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/tvProfileName"
        android:layout_below="@+id/ivProfilePicture"
        android:layout_marginTop="123dp"
        android:layout_toLeftOf="@+id/btnChangeName"
        android:ems="10"
        android:hint="Profile Name" >

        <requestFocus />
    </EditText>

    <TextView
        android:id="@+id/tvProfilePictureTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:text="Profile Picture"
        android:textAppearance="?android:attr/textAppearanceLarge" />

    <Button
        android:id="@+id/btnChangePicture"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/etProfileName"
        android:layout_alignParentRight="true"
        android:onClick="changePicture"
        android:text="Change Picture" />

    <TextView
        android:id="@+id/tvProfileName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/btnChangeName"
        android:layout_alignParentLeft="true"
        android:layout_alignParentRight="true"
        android:text="Profile Name"
        android:textAppearance="?android:attr/textAppearanceLarge" />

    <Button
        android:id="@+id/btnChangeName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/etProfileName"
        android:layout_alignBottom="@+id/etProfileName"
        android:layout_alignLeft="@+id/btnChangePicture"
        android:layout_alignRight="@+id/tvProfileName"
        android:onClick="saveName"
        android:text="Save Name" />

    <ImageView
        android:id="@+id/ivProfilePicture"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_alignLeft="@+id/tvProfilePictureTitle"
        android:layout_below="@+id/tvProfilePictureTitle"
        android:layout_marginTop="5dp"
        android:src="@drawable/kid" />

</RelativeLayout>